# JSON Bookmarks Page Generator

This is a Python program that can generate a bookmarks page from a `.json` file. Modify `data.json` to your liking, run `main.py`, and see the results in `start.html`!
